# URL where the Python inference API is running (see backend/app.py)
VITE_API_BASE_URL=http://localhost:5000

# Set to "true" to develop the UI with fake predictions, without a running backend
VITE_USE_MOCK=false
